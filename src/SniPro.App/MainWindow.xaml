<Window x:Class="SniPro.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SniPro Settings"
        Height="440"
        Width="560"
        MinHeight="440"
        MinWidth="560"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="26"
                   FontWeight="SemiBold"
                   Text="SniPro Settings" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Output directory" />
            <TextBox x:Name="OutputDirectoryTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,8,12" />
            <Button x:Name="BrowseButton" Grid.Row="0" Grid.Column="2" Margin="0,0,0,12" Padding="12,4" Content="Browse..." Click="BrowseButton_Click" />

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Frame rate (FPS)" />
            <TextBox x:Name="FrameRateTextBox" Grid.Row="1" Grid.Column="1" Width="100" HorizontalAlignment="Left" Margin="0,0,0,12" />

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Duration (seconds)" />
            <TextBox x:Name="DurationTextBox" Grid.Row="2" Grid.Column="1" Width="100" HorizontalAlignment="Left" Margin="0,0,0,12" />

            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Output scale (%)" />
            <TextBox x:Name="ScalePercentTextBox" Grid.Row="3" Grid.Column="1" Width="100" HorizontalAlignment="Left" Margin="0,0,0,12" />

            <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Maximum colors" />
            <ComboBox x:Name="MaxColorsComboBox" Grid.Row="4" Grid.Column="1" Width="110" HorizontalAlignment="Left" Margin="0,0,0,12" />

            <CheckBox x:Name="EnableDitheringCheckBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,4" Content="Enable dithering" />
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ResetButton" Margin="8,0,0,0" Padding="16,6" Content="Reset" Click="ResetButton_Click" />
            <Button x:Name="SaveButton" Margin="8,0,0,0" Padding="16,6" Content="Save" Click="SaveButton_Click" />
            <Button x:Name="CloseButton" Margin="8,0,0,0" Padding="16,6" Content="Close" Click="CloseButton_Click" />
        </StackPanel>

        <TextBlock x:Name="StatusText"
                   Grid.Row="5"
                   Margin="0,12,0,0"
                   Foreground="Gray"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
